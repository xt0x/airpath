{
  "name": "@airpath/shared-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "fixtures",
    "src"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "lint": "tsc --project tsconfig.json --noEmit --noUnusedLocals --noUnusedParameters && tsc --project tsconfig.test.json --noEmit --noUnusedLocals --noUnusedParameters && tsc --project tsconfig.fixtures.json --noEmit --noUnusedLocals --noUnusedParameters",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --passWithNoTests",
    "typecheck": "tsc --project tsconfig.json --noEmit && tsc --project tsconfig.test.json --noEmit && tsc --project tsconfig.fixtures.json --noEmit"
  },
  "devDependencies": {}
}
